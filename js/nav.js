


//--------------trending slider-----------


/*---------------------------

const nextSlide = document.querySelector(".next-slide")
const prevSlide = document.querySelector(".prev-slide")

nextSlide.addEventListener("click", function(e) {
   // console.log(e.currentTarget.parentElement.parentElement.children[1].children[0])

    let elem = e.currentTarget.parentElement.parentElement.children[0]
    let item = elem.getElementsByClassName("trending-box")
    elem.append(item[0])
  

})

prevSlide.addEventListener("click", function(e){
    let elem = e.currentTarget.parentElement.parentElement.children[0]
    let item = elem.getElementsByClassName("trending-box")
    elem.prepend(item[item.length-1])

})

-----------------*/



//----X----------trending slider------X-----